@using System.Security.Claims;

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@{
    string? username = User.FindFirstValue(ClaimsNames.Fullname);
    string? id = User.FindFirstValue(ClaimsNames.Id);

    string? isAdminAsString = User.FindFirstValue(ClaimsNames.IsAdmin);
    bool isAdmin = isAdminAsString != null ? Boolean.Parse(isAdminAsString) : false;
}

<ul class="navbar-nav">
    @if (username == null)
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Users" asp-action="Login" asp-route-returnurl="@Context.Request.Path">Login</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Users" asp-action="Register" asp-route-returnurl="@Context.Request.Path">Register</a>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Users" asp-action="Details">Welcome @username</a>
        </li>
        @if (isAdmin)
        {
             <li class="nav-item">
                <a class="nav-link text-dark" asp-controller="Administration" asp-action="Index">Administration area</a>
            </li>   
        }        
        <li class="nav-item">
            <form method="post" asp-controller="Users" asp-action="Logout" asp-route-returnurl="@Context.Request.Path">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>           
        </li>
    }   
</ul>
